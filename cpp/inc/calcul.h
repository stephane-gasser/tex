#ifndef MULTANDADD_H
#define MULTANDADD_H

#include "globals.h"

inline bool aritherror;
inline scaled remainder_;

scaled multandadd(int, scaled, scaled, scaled);
scaled nx_plus_y(int, scaled, scaled);
scaled mu_mult(int, scaled, int);
scaled mult_integers(int, scaled);
scaled xnoverd(scaled, int, int);
scaled xovern(scaled, int);

#endif
